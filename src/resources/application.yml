spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ad_manger?useUnicode=true&amp;autoReconnect=true&amp;characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    tomcat:
      max-active: 20
      max-idle: 10
      max-wait: 60000
      min-idle: 5
      initial-size: 5
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.hjh.entity
  configuration:
    call-setters-on-nulls: true
logging:
  level:
   com:
     hjh:
      dao: debug
server:
  port: 9090
